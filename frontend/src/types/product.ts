export type Product = {
    id: number,
    description: string,
    image: string,
    price: number,
    quantity: number,
    validate: string,
    measureValue: number,
    measure: string,
    category: number
}